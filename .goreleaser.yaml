project_name: anarchy-solo-challenge

builds:
  - main: ./main.go
    binary: anarchy-solo-challenge
    goos: [linux, windows, darwin]
    goarch: [amd64, arm64]

archives:
  - format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt
